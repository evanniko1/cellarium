# Corpus ledger

Every dropped run, recorded so a decision stays auditable across DB versions (WELL-1y). Dropping tombstones the manifest row (never deletes it) and frees disk by removing the RAW simOut only. Newest last.

| when (UTC) | design | id | reason |
|---|---|---|---|
| 2026-08-06 05:01 | gene_knockout/KO:alaS | `gene_knockout_1_e4a7ece7` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:argS#elong:kinetic | `gene_knockout_3_4545c219` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:pheS | `gene_knockout_0_a7747e11` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:gltX | `gene_knockout_2_9d5e1ce3` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:alaS | `gene_knockout_2_c19c7305` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:gltX | `gene_knockout_0_9d7dc5a8` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:argS#elong:kinetic | `gene_knockout_2_7fad4abd` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:argS | `gene_knockout_3_b6f94703` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:gltX | `gene_knockout_3_02bc9b48` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:argS#elong:kinetic | `gene_knockout_1_dbc2b1ac` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:argS | `gene_knockout_1_56bc58cd` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:alaS | `gene_knockout_3_acde7d4e` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:pheS | `gene_knockout_1_b1670b12` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:argS | `gene_knockout_0_f34ad1bc` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:gltX | `gene_knockout_1_919eb66c` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:alaS | `gene_knockout_0_9ad0b58c` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:argS#elong:kinetic | `gene_knockout_0_45ddf714` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:argS | `gene_knockout_2_6c787763` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:pheS | `gene_knockout_2_5a181cc7` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
| 2026-08-06 05:01 | gene_knockout/KO:pheS | `gene_knockout_3_889f460e` | MISLABELLED KNOCKOUT (WELL-NOOP-1 class). Launched via Design(condition='KO:<gene>') with no params['variant_index'], so runner._variant_index fell back to a content hash. gene_knockout.py takes geneIndex=(index-1)%(len(rna_data)+1), so the hash silenced an unrelated transcription unit: argS hit TU0-14745 not TU0-13444, alaS TU0-8508 not TU0-6441, pheS TU0-8778 not TU0-14257, gltX TU4 not TU0-6409. The rows carry a knockout label over a different perturbation entirely. Re-run with explicit indices argS=644, alaS=2078, gltX=2074, pheS=1340. |
